import React from 'react';
import classes from './Logo.module.css';

const Logo = (prop) => {
    return ( 
        <div className={classes.Logo}>
            <span>Spha</span>
        </div>
     );
}
 
export default Logo;